// Bump this number every time you build and share a new APK.
// It must match the "latestVersion" you set in Firestore (config/appVersion)
// when you want everyone to be notified of that build.
export const CURRENT_VERSION = '1.0.0';
