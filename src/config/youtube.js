/* ---------------------------------------------------------------
   👉 YouTube Data API v3: https://console.cloud.google.com/apis/library/youtube.googleapis.com
   1. In Google Cloud Console, enable "YouTube Data API v3" for your project
   2. Create an API key under APIs & Services → Credentials
   3. Paste it below

   ⚠️ Your GitHub repo is public, so this key is visible to anyone who
   looks at the code — same situation as the Firebase/Pexels keys.
   Restrict this key to the YouTube Data API only (Credentials → your
   key → API restrictions) to limit what it can be used for.

   Free quota: 10,000 units/day. Each search here costs 100 units, so
   roughly 100 loads/day total across everyone using the app before
   Discover stops loading until the quota resets.
---------------------------------------------------------------- */
export const YOUTUBE_API_KEY = 'AIzaSyAR9a1Fj3oZHhma-y57INGpcNBOdW0RGS0';
